* text=auto

# Windows script hosts need CRLF, so pin it regardless of platform
*.bat text eol=crlf
*.cmd text eol=crlf
*.vbs text eol=crlf
*.ps1 text eol=crlf

# never line-ending-convert the icon
*.ico binary
*.png binary
